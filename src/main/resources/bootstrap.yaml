server:
    port: 5678

spring:
    profiles:
        active:
            - prod
    application:
        name: config
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/TimGundmann/gundmann-config